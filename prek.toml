# prek configuration for vmenu
# https://prek.j178.dev/

# Built-in fast hooks (Rust-native, zero setup, offline capable)
[[repos]]
repo = "builtin"
hooks = [
  { id = "trailing-whitespace" },
  { id = "end-of-file-fixer" },
  { id = "check-merge-conflict" },
  { id = "check-added-large-files", args = ["--maxkb=1024"] },
  { id = "detect-private-key" },
  { id = "check-case-conflict" },
]

# Local project hooks
[[repos]]
repo = "local"

[[repos.hooks]]
id = "swiftlint"
name = "swiftlint"
language = "system"
entry = "swiftlint lint --strict --quiet"
files = '\.swift$'

[[repos.hooks]]
id = "swift-build"
name = "swift build"
language = "system"
# Build with complete concurrency checking so data-race / Sendable errors
# that the CI toolchain enforces are caught locally before pushing, rather
# than only surfacing in CI on a newer/stricter Swift toolchain.
entry = "swift build -Xswiftc -strict-concurrency=complete"
pass_filenames = false
always_run = true

[[repos.hooks]]
id = "conventional-commit"
name = "conventional commit"
language = "script"
entry = "scripts/check-conventional-commit.sh"
stages = ["commit-msg"]
always_run = true
